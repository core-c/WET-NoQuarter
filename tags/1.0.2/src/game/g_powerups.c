#include "g_local.h"
/*
syscall(G_TRACE,
		results,
		start,
		mins,
		maxs,
		end,
		passEntityNum,
		contentmask);
*/
G_PutEntAtCrosshair(gentity_t *ent, gentity_t *viewer)
{
	trace_t tr;
	vec3_t	start, end;

//	trap_Trace(&tr, start, NULL, NULL, end, );

	
}

